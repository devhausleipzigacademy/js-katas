function addProperty(object, key, value) {
  if ( /^\d|[^\w\$]/.test(key) ) {
    // your code goes here; log the proper message to the console to let the caller know they are using a non-standard identifier
  }
  // your code goes here; add the property to the object
};
