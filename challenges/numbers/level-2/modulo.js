var nextItemIndex = function(currentItemIndex, numItems) {
  // compute the next Item, based on a round robin scheme.
  // That means if you reach the end of the Items, start again from the start with 0
  return currentItemIndex;
};
